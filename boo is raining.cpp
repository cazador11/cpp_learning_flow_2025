#include <iostream>
/**
 * @brief Prints the value of isRaining to stdout
 *
 * @details
 * This function prints the value of isRaining, which is a boolean
 * variable, to stdout. The value of isRaining is set to false.
 *
 * @return 0 if successful
 */
int main(){
    bool isRaining = false;

    std::cout<<"is raining "<<isRaining<<std::endl;
}